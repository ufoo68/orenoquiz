import { type NextPage } from "next";

const Question: NextPage = () => {

  return (
    <div className="w-screen h-screen bg-neutral-200 flex justify-center flex-col items-center">
      question
    </div>
  );
};

export default Question;
